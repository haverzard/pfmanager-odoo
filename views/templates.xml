<odoo>
    <data>
        <template id="homepage">
            <head>
                <title>PFManager</title>
                <style>
                    * {box-sizing: border-box;}

                    body {
                      margin: 0;
                      font-family: Arial, Helvetica, sans-serif;
                    }

                    .header {
                      overflow: hidden;
                      background-color: #f1f1f1;
                      padding: 20px 10px;
                    }

                    .header a {
                      float: left;
                      color: black;
                      text-align: center;
                      padding: 12px;
                      text-decoration: none;
                      font-size: 18px;
                      line-height: 25px;
                      border-radius: 4px;
                    }

                    .header a.logo {
                      font-size: 25px;
                      font-weight: bold;
                    }

                    .header a:hover {
                      background-color: #ddd;
                      color: black;
                    }

                    .header a.active {
                      background-color: dodgerblue;
                      color: white;
                    }

                    .header-right {
                      float: right;
                    }

                    @media screen and (max-width: 500px) {
                      .header a {
                        float: none;
                        display: block;
                        text-align: left;
                      }

                      .header-right {
                        float: none;
                      }
                    }
                </style>
            </head>

            <div class="header">
              <a href="#default" class="logo">PFManager</a>
              <div class="header-right">
                <a href="#home">Proyek</a>
                <a href="#contact">Evaluasi</a>
                <a href="#about">Pengaturan</a>
              </div>
            </div>

            <div style="padding-left:20px">
              <h1>Test</h1>
              <p>Test</p>
              <p>LOREM IPSUM</p>
                <form method="POST" action="/proyek">
                    <input id="nama" name="nama" required="true"/>
                    <button type="submit">Submit</button>
                </form>
            </div>
        </template>

        <template id="listing">
          <ul>
            <li t-foreach="objects" t-as="object">
              <a t-attf-href="#{ root }/objects/#{ object.id }">
                <t t-esc="object.display_name"/>
              </a>
            </li>
          </ul>
        </template>

        <template id="object">
          <h1><t t-esc="object.display_name"/></h1>
          <dl>
            <t t-foreach="object._fields" t-as="field">
              <dt><t t-esc="field"/></dt>
              <dd><t t-esc="object[field]"/></dd>
            </t>
          </dl>
        </template>

    </data>
</odoo>